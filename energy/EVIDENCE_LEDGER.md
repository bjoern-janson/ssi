# SSI Energy Suite — Frozen Evidence Ledger

Status: **FROZEN after V2.2 routing-branch closure; before any identified V3 treatment run.**

This ledger records the earned empirical state of the isolated `energy/` suite. It does not modify SSI core theory.

## Status vocabulary

- `SUPPORTED`: the preregistered hypothesis was measured and its support criterion passed within frozen scope.
- `NOT_SUPPORTED`: the preregistered hypothesis was measured and its support criterion did not pass within frozen scope.
- `NOT_IDENTIFIED`: the target estimand was not validly measured. This is not zero effect, negative effect, or positive effect.

These statuses are not interchangeable.

## Experiment ledger

| Experiment | Target | Status |
|---|---|---|
| V0 | persistent lineage reduces synthetic corrective traversal | `SUPPORTED` |
| V1 | traversal reduction survives independently executed CPU work and lineage overhead | `SUPPORTED` |
| V2 | lineage reduces total executed corrective computation under heterogeneous candidate costs | `SUPPORTED` |
| V2.1 | unconditional `q/c` cost-aware routing improves total executable economy over similarity-only routing | `NOT_SUPPORTED` |
| V2.2 | nearest-lineage-distance-gated `q/c` routing improves total executable economy over similarity-only routing | `NOT_SUPPORTED` |
| V3 | V2 computational economy reduces measured physical energy | `NOT_IDENTIFIED` |

## Earned claim

Within the frozen synthetic executable scope:

> **Validated persistent lineage reduces heterogeneous corrective computation.**

The supported mechanism is localized to the original V2 intervention:

\[
\Lambda_{\rm preserved}
\rightarrow
n_{\rm tested}\downarrow
\rightarrow
C_{\rm heterogeneous,actual}\downarrow.
\]

V2.1 and V2.2 do not enter this earned mechanism. They tested additional routing policies and failed their preregistered net-economy criteria.

## Routing-branch localization

The routing branch is closed.

Supported:

\[
\Lambda\rightarrow q_\Lambda,
\]

and in V2.1:

\[
(q_\Lambda,\hat c)\rightarrow C_{\rm eval}\downarrow.
\]

Not supported:

\[
(q_\Lambda,\hat c)\rightarrow C_{\rm total}\downarrow,
\]

and:

\[
d_{\min}\rightarrow\text{positive routing ROI}.
\]

No V2.3, threshold retuning, additional gate features, or post-hoc subgroup rescue is licensed by this suite.

## Open bridge

The only remaining energy bridge is:

\[
C_{\rm heterogeneous,actual}\downarrow
\overset{?}{\longrightarrow}
E_{\rm physical}\downarrow.
\]

Until an admissible physical-energy boundary is available:

\[
\Delta E_{\rm physical}=\texttt{NOT\_IDENTIFIED}.
\]

CPU time, wall time, TDP, synthetic costs, and inferred joules are not substitutes for physical-energy measurement.

## Authority boundary

Energy results may update only the energy mechanism within the intervention and measurement scope that identified them. They cannot retroactively redefine corrective topology, Axis I, or SSI core theory.
