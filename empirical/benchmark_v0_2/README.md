# VFA-0.2 — Dormant Corrective Reserve

## Lineage identity

```text
VFA-0.1-REJECTED_RETRIEVAL_LEAKAGE
    ->
VFA-0.2-DORMANT-CORRECTIVE-RESERVE
```

VFA-0.2 is a **new construction lineage**, not a repaired scramble seed.

The VFA-0.1 construction showed that genuine similarity/provenance topology changed precedent retrieval and therefore ordinary adaptability before any future obligation. That construction remains preserved and rejected at D.

VFA-0.2 factorizes the causal object:

\[
\mathcal K_t=(R_t,F_t,D_t)
\]

with the intended construction

\[
R_t^A=R_t^B,\qquad
F_t^A=F_t^B,\qquad
D_t^A\ne D_t^B.
\]

`D_t` is dormant corrective reserve structure: provenance, challenge, alternative-preservation and reopenability relations. It is **not** extra compute, extra candidate count, a larger search budget, or a better ordinary retrieval policy.

## Defining invariant

> **VFA-0.2 is not allowed to demonstrate treatment advantage before reserve activation.**

Formally,

\[
G_{\rm reserve}=0
\Rightarrow
\frac{\partial T_{\rm forward}}{\partial D}=0
\]

and

\[
G=g(E_{\rm insuff}),
\qquad
\frac{\partial G}{\partial D}=0.
\]

The stronger architectural interpretation is:

\[
D\nrightarrow T_{\rm forward},
\qquad
D\nrightarrow G,
\]

where the prohibited arrow includes a dereference-capable interface path, not only an observed behavioral effect.

## Current construction status

```text
LINEAGE = VFA-0.2-DORMANT-CORRECTIVE-RESERVE
FUTURE_OBLIGATION_ACCESSED = FALSE

D_PRE_ACTIVATION_INITIAL = PASS
BEHAVIORAL_HOSTILE_NONINTERFERENCE = PASS
D_PRE_ACTIVATION_HARDENED = FAIL
FAILURE_CODE = OPEN_CAPABILITY_SURFACE

FREEZE_PACKET = NOT_FROZEN
AUTHORIZATION_CERTIFICATE = NOT_ISSUED
FUTURE_RUN = NOT_AUTHORIZED
```

This is **not** an A-I authorization certificate and is not a scientific result about future adaptation.

## Initial pre-activation result

The first leakage suite used the inherited frozen Biome migration-class corpus and the same genuine forward retrieval topology for both arms.

Across the exhaustive 249-task pre-freeze surrogate universe:

- A/B use the same forward state and same solver;
- 10 reserve presentations/mutations are attacked per task;
- total forward-trace comparisons: **2,490**;
- material trace mismatches: **0**;
- gate mismatches: **0**;
- `Q_adapt` is exactly equal;
- reserve A/B retain the same nodes, edge count, edge-kind multiset, and canonical serialized byte length;
- reserve topology separates on `C_challenge`, `L_prov`, and `R_reopen`;
- `A_preserve` remains equal at `1.0` because both arms retain the same alternative quantity.

That initial certificate remains preserved as a valid result of its narrower attack surface.

## Hostile factorization attack 1

The second attack deliberately tried to bend the factorization rather than progress toward `G=1`.

It generated **32** additional contract-valid reserve geometries, each preserving the same cases, binding kinds, 24-edge quantity, source/target multisets, and **587-byte** canonical payload length. Across 249 tasks it varied four semantically equivalent query presentations and three equal caller-side allocation/serialization pressure levels.

Results:

```text
forward metamorphic comparisons = 101592
forward trace mismatches         = 0
module-global poisoning checks   = 2988
module-global mismatches         = 0
gate behavioral comparisons      = 680
gate mismatches                  = 0
transitive static call audit     = PASS
```

Thus the reserve still showed no behavioral effect under this harder deterministic attack.

However, the stronger architectural certificate failed. Both protected functions accept open-ended dictionaries:

```text
forward_trace(query, shared_forward)
evaluate_gate(evidence)
```

A reserve-bearing extra field was accepted rather than rejected:

```text
forward reserve-smuggling attempts = 498
accepted                           = 498
behavior changes                   = 0

gate reserve-smuggling attempts    = 10
accepted                            = 10
behavior changes                    = 0
```

The current code therefore establishes **behavioral non-use**, but not **architectural non-capability**.

\[
\boxed{
D\text{ is ignored}
\;\not\Rightarrow\;
D\text{ is inaccessible}
}
\]

The failure is localized to the interface/capability surface. It does not reopen VFA-0.1 and it is not evidence against the SSI future-adaptation hypothesis.

## Architectural boundary under revision

The intended rule remains:

\[
G=0
\Rightarrow
F_{\rm forward}\text{ and }G\text{ receive no reserve-capable object.}
\]

The current generic dictionary interfaces are too permissive to certify that stronger statement even though their implementations do not read reserve fields.

The next construction step must close the ordinary-forward and insufficiency-evidence schemas, then rerun both the original and hostile leakage attacks. No reserve activation is licensed by the current state.

## Frozen trace equivalence

Ordinary-operation equivalence remains trace-level, not endpoint-only:

\[
T_A(x)\equiv_{\mathcal T_{\rm prereg}}T_B(x).
\]

The trace fields and admissible equivalence relation are frozen in `construction/TRACE_EQUIVALENCE.json`. The relation is `IDENTITY_ONLY`; no result-dependent normalization is permitted.

## Files

- `CONSTRUCTION_INVARIANTS.md` — causal and adjudication invariants.
- `construction/SHARED_FORWARD.json` — common ordinary forward state.
- `construction/reserve_A.json` / `reserve_B.json` — equal-quantity, different-binding dormant reserves.
- `construction/reserve_interface.py` — current architectural gate boundary.
- `construction/TRACE_EQUIVALENCE.json` — frozen trace identity.
- `construction/GATE_SPEC.json` — arm-independent insufficiency gate.
- `construction/PREACTIVATION_LEAKAGE_SUITE.py` — initial adversarial construction audit.
- `construction/preactivation_leakage_audit.json` — initial machine-readable result.
- `construction/HOSTILE_FACTORIZATION_ATTACK.py` — harder metamorphic/capability attack.
- `construction/hostile_factorization_audit.json` — hostile attack result.
- `evidence/HOSTILE_FACTORIZATION_ATTACK_1.md` — preserved failure interpretation.

## Stop condition

No prospective future obligation is to be inspected, selected, simulated, or executed from this lineage.

Current next action is **interface hardening and re-attack only**. The freeze packet remains unfrozen and the future run remains unauthorized.
