# Empirical Program Index

This directory contains multiple **scientific lineages**, including failed and superseded constructions. Files are preserved for causal attribution and provenance; a later directory does not retroactively rewrite an earlier result.

For repository-wide status and PR topology, see [`../REPOSITORY_STATUS.md`](../REPOSITORY_STATUS.md).

## Lineage map

```text
V0.x synthetic localization              CLOSED
        |
        v
Independent future-adaptation contract
        |
        +-- VFA-0.1                        REJECTED AT D
        |
        +-- VFA-0.2 dormant reserve        PRESERVED / SUPERSEDED
        |
        +-- VFA-0.2 quotient topology      ACTIVE AUTHORIZED LINEAGE
```

## `INDEPENDENT_FUTURE_ADAPTATION.md`

Program-level design document for the move from synthetic localization to independent future correction demands.

It should be read as the **general research target and preregistration discipline**, not as the current VFA status ledger. The concrete benchmark has since progressed through several construction failures and repairs to an authorized quotient-topology instance.

## `benchmark_v0_1/`

First concrete independent VFA construction.

Scientific outcome:

```text
A = PASS
B = PASS
C = PASS
D = FAIL
E-I = NOT_EVALUATED
```

Failure localization:

```text
VFA-0.1-REJECTED_RETRIEVAL_LEAKAGE
TREATMENT-MECHANISM LEAKAGE INTO ORDINARY ADAPTATION
```

The construction showed that its similarity topology was itself ordinary adaptive capability. The failure is preserved; later VFA constructions do not convert it into a pass.

## `benchmark_v0_2/`

Dormant-corrective-reserve construction lineage.

This directory is historically important because it separated behavioral non-use from capability-surface closure and exposed two distinct design problems:

1. the original reserve A/B representation could conflate revision topology with semantic truth/provenance validity;
2. the first hostile capability attack found `OPEN_CAPABILITY_SURFACE` despite behavioral noninterference.

The directory is therefore **preserved scientific history**, not the final VFA treatment identity.

## `benchmark_v0_2_quotient/`

Current prospective VFA lineage.

Treatment:

```text
W_A = W_B = W
Gamma_B = q(Gamma_A)
F o q = F
```

Both arms share the same validated factual substrate and ordinary behavior; they differ only in revision-path equivalence structure.

Current authoritative state is in:

- [`benchmark_v0_2_quotient/AUTHORIZATION_STATUS.json`](benchmark_v0_2_quotient/AUTHORIZATION_STATUS.json)
- [`benchmark_v0_2_quotient/construction/I_FREEZE_PACKET_V7.json`](benchmark_v0_2_quotient/construction/I_FREEZE_PACKET_V7.json)
- [`benchmark_v0_2_quotient/construction/I_AUTHORIZATION_CERTIFICATE.json`](benchmark_v0_2_quotient/construction/I_AUTHORIZATION_CERTIFICATE.json)

Current state:

```text
A-I = PASS
PACKET_7 = FROZEN
AUTHORIZATION_CERTIFICATE = ISSUED
STATE = AUTHORIZED_FUTURE_NOT_YET_REALIZED
FUTURE_OBLIGATION_ACCESSED = FALSE
FUTURE_RUN = NOT_EXECUTED
```

The primary prospective question is:

\[
\ker q\stackrel{?}{\subseteq}\ker T_{\rm future}.
\]

The first inferential unit is the treatment-blind future-distinction grounding over **3 independent source-fact pairs**, reported through **12 deterministic relation-kind surfaces**. Those twelve surfaces are not twelve independent observations.

## Authority order inside the quotient directory

Several root-level files in `benchmark_v0_2_quotient/` describe earlier audit states and remain intentionally preserved. For current status, use this precedence:

1. `AUTHORIZATION_STATUS.json` — current machine-readable state;
2. `construction/I_AUTHORIZATION_CERTIFICATE.json` — exact authorized object;
3. `construction/I_FREEZE_PACKET_V7.json` and `I_FREEZE_ANCHOR_V7.json` — frozen identity;
4. predicate-specific evidence and audit JSON;
5. older re-audit/addendum/candidate status files — historical context only.

Do not infer current authority from the newest-looking filename or from an older README paragraph.

## Post-authorization material

The following live outside the authorized Packet 7 execution object:

- [`../research/post_authorization/VFA_0_2_BLIND_OUTCOME_ADJUDICATION.json`](../research/post_authorization/VFA_0_2_BLIND_OUTCOME_ADJUDICATION.json) — pre-realization interpretation map;
- [`../theory/FUTURE_SAFE_OPTION_STRUCTURE.md`](../theory/FUTURE_SAFE_OPTION_STRUCTURE.md) — Future-Safe Option Structure theory note.

They may constrain later narrative interpretation but may not change Packet 7, its selector, grounding, evaluator, execution closure, or authorization.

## Preservation rule

> **Failed construction is evidence about the generating process. Preserve it; route around it clearly.**

The empirical cleanup policy is therefore additive indexing rather than destructive refactoring.
