# H Evidence — Residual-Confound Veto

## Adjudication

```text
LINEAGE = VFA-0.2-H-FAIL-POSTGATE-SEMANTIC-UNDERDETERMINATION
H       = FAIL
```

This is a pre-freeze authorization failure for `VFA-0.2-QUOTIENT-REVISION-TOPOLOGY`.

It does **not** access a real future obligation, activate `G`, evaluate `DeltaPi`, freeze the packet, or authorize a run.

## Scope rule

H is not an audit of every imaginable difference between two objects.

The frozen rival universe is:

\[
\mathcal H_{\rm material}
=
\{H_j:\;H_j\text{ has a plausible causal path capable of changing the first post-gate reachability estimand under the frozen A--G design}\}.
\]

Imaginability alone does not grant veto authority.

Each admitted rival receives one disposition:

```text
BLOCKED
MEASURED
RANDOMIZED
RESIDUAL
```

A residual vetoes authorization iff:

\[
\boxed{
\text{Material}(H_j)
\land
\text{Unbounded}(H_j)
\land
\text{EffectExplaining}(H_j)
}
\]

## Ledger result

The ledger contains the twelve mandatory rivals from the governing V0.1 contract plus quotient-specific rivals introduced by the redesigned treatment.

```text
rivals                     = 16
BLOCKED                    = 9
MEASURED                   = 1
RESIDUAL                   = 6
unclassified               = 0
missing mandatory rivals   = 0
evidence gaps in BLOCKED / MEASURED rows = 0
ledger completeness        = PASS
```

Three residuals satisfy the veto.

## Veto 1 — H_quotient_interpretation

Causal entry:

\[
\Gamma\rightarrow\Delta\Pi.
\]

A--G establish what the treatment *is*, when it may become accessible, and what common evidence/resources accompany it. They do not yet freeze the semantic operator that is allowed to transform the activated equivalence relation into candidate probes.

Therefore an implementation could, for example, reward singleton count, class density, row sums, or another deterministic function of `M_Gamma` and generate an A-favoring `DeltaPi` without operationalizing preserved revision-path reachability.

That rival is:

```text
Material        = TRUE
Unbounded       = TRUE
EffectExplaining= TRUE
Disposition     = RESIDUAL
Veto            = TRUE
```

This is not a complaint that `M_Gamma` differs. `M_Gamma` *is* the treatment. The problem is that the treatment-to-probe semantic map remains underdetermined.

## Veto 2 — H_challenge_recognizability

Causal entry:

\[
(E_{\rm insuff},\Gamma)\rightarrow\Delta\Pi.
\]

G establishes common-cause identity of the failure evidence:

\[
E_{\rm insuff}^{A}=E_{\rm insuff}^{B}.
\]

But common evidence does not by itself establish a common semantic interpretation rule after activation.

Without a frozen arm-independent interpretation operator, identical failure evidence could be decoded differently conditional on the quotient representation, and that differential decoding could explain the entire predicted reachability contrast.

```text
Material        = TRUE
Unbounded       = TRUE
EffectExplaining= TRUE
Disposition     = RESIDUAL
Veto            = TRUE
```

## Veto 3 — H_evaluator

Causal entry:

\[
\Delta\Pi\rightarrow\operatorname{Reach}(\Delta\Pi).
\]

The project has frozen that the first scientific endpoint is probe-space reachability, but it has not yet frozen an executable identity rule for a probe, the admissible representation transformations, or the exact reachability evaluator.

An evaluator could therefore prefer one representation of a valid probe, count duplicate/renamed probes asymmetrically, or otherwise manufacture an A/B contrast.

```text
Material        = TRUE
Unbounded       = TRUE
EffectExplaining= TRUE
Disposition     = RESIDUAL
Veto            = TRUE
```

## Non-veto residuals

H deliberately preserves residuals that lack causal authority over the first estimand.

`H_future_distribution` is unbounded for transport/generalization, but because G supplies one paired common event it cannot by itself create within-event A/B exposure differences.

`H_CCA_CARS_downstream` remains open for later warrant/authority claims, but lies downstream of the first `DeltaPi` reachability endpoint.

`H_physical_runtime` remains outside the benchmark's logical-budget estimand unless physical runtime is later allowed to control stopping or success; such a change would reopen E and H.

Thus `RESIDUAL` alone is not failure. The veto is causal and estimand-specific.

## Failure localization

\[
\boxed{
\texttt{POSTGATE\_SEMANTIC\_UNDERDETERMINATION}
}
\]

A--G have isolated:

```text
same validated substrate
same ordinary causal behavior
same logical resource environment
same hidden-asymmetry boundary
same prospective obligation and disclosure mechanism
```

but have not yet identified:

\[
\boxed{
\Psi_{\rm post}:
(M_\Gamma,E_{\rm insuff},\mathcal W)
\longrightarrow
\Delta\Pi
}
\]

or the exact evaluator:

\[
\boxed{
\mathcal V_{\Pi}:\Delta\Pi\longrightarrow\operatorname{Reach}(\Delta\Pi).
}
\]

Those are now the shallowest unresolved objects.

## Required repair before H may be re-audited

The next construction must freeze, without future-obligation information:

1. the arm-independent post-gate input schema;
2. the semantic rule by which common insufficiency evidence addresses the frozen revision-path substrate;
3. the legal operations on `M_Gamma` for generating candidate probes;
4. a rule preventing arbitrary topology-summary scoring from masquerading as path reachability;
5. probe identity and equivalence under admissible representation transformations;
6. the exact `DeltaPi` reachability evaluator;
7. duplicate, missing, invalid, abstention, and stopping semantics;
8. a hostile synthetic/metamorphic suite showing that representation-preserving rewrites cannot change reachability while genuine path collapses can.

Only after those artifacts exist may the three veto rows be reclassified and H re-run.

## Stop condition

```text
A = PASS
B = PASS
C = PASS
D = PASS
E = PASS
F = PASS
G = PASS
H = FAIL
I = NOT_EVALUATED

FUTURE_OBLIGATION_ACCESSED = FALSE
G_ACTIVATION               = PROHIBITED
DELTA_PI                    = NOT_EVALUATED
FREEZE_PACKET               = NOT_FROZEN
AUTHORIZATION_CERTIFICATE  = NOT_ISSUED
FUTURE_RUN                  = NOT_AUTHORIZED
```

No I. No freeze. No future obligation. No activation.
