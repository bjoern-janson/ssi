# F reproduction note

The predicate-F attack is deterministic and construction-side. Its result artifact was reproduced from the fetched branch inputs and the frozen residual-feature/classifier definitions before being recorded in `construction/f_treatment_isolation_audit.json`.

This note does **not** claim a GitHub Actions or external CI execution. The authoritative scientific claim is the content-addressed construction evidence and exact paired residual identity criterion; rerunning `construction/F_TREATMENT_ISOLATION_ATTACK.py` in the frozen benchmark runtime is expected to regenerate the same adjudication.

No future obligation was accessed. `G` remained prohibited.
