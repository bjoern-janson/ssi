# H reproduction note

The H adjudication follows deterministically from the frozen material-rival manifest and veto rule.

The ledger arithmetic was independently reproduced before recording `construction/h_residual_confound_audit.json`:

```text
rivals        = 16
BLOCKED       = 9
MEASURED      = 1
RESIDUAL      = 6
unclassified  = 0
veto rows     = 3
```

The veto rows are:

```text
H_evaluator
H_quotient_interpretation
H_challenge_recognizability
```

This note does not claim a GitHub Actions or external-CI run. The audit logic is purely construction-side and accesses no future obligation. `G` remains prohibited.
