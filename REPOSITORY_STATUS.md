# SSI Repository Status

**Administrative snapshot:** 2026-08-17  
**Scientific authority:** frozen artifacts and their provenance, not this summary.

For the exact current stop, see [`research/CURRENT_FRONTIER.md`](research/CURRENT_FRONTIER.md).

---

## Executive status

The repository no longer stops conceptually at PR61. The current bounded lineage is:

```text
PR59  ROLE_SEPARATION
PR60  TESTED_STRUCTURAL_TRANSPORT
PR61  TRANSITION_SEMANTIC_TRANSPORT
PR63  ADEQUACY / REOPENING GOVERNOR
PR64  H9 POSITIVE-ADEQUACY CONSTITUTION COLLISION
PR65  NEGATIVE ONE-BIT CANDIDATE SEARCH
PR67  EXTERNAL-TO-SSI MAPPING CONTRACT M_F [draft / branch-local]
```

PR63–65 are mainline-reachable. PR67 remains an open draft and is not silently promoted into mainline scientific authority by this file.

Current strongest bounded claims include:

```text
FOUR_COORDINATE_TRANSITION_INTERFACE_SEPARABILITY
    = SUPPORTED_ON_FROZEN_CONSTRUCTED_SUITE

TESTED_STRUCTURAL_TRANSPORT
    = SUPPORTED_ON_EVALUABLE_FROZEN_HELDOUT_AXES

TRANSITION_SEMANTIC_TRANSPORT
    = SUPPORTED_ON_FROZEN_CONSTRUCTED_CROSS_CARRIER_SUITE

POSITIVE_ADEQUACY_CONSTITUTION_NONIDENTIFIABILITY
    = SUPPORTED_IN_H9

EXTERNAL_TO_SSI_MAPPING_CONSTITUTION
    = SUPPORTED_ON_FROZEN_ABSTRACT_MAPPING_SUITE
      __PR67_BRANCH_LOCAL
```

The path-level composition question remains open.

---

## Critical correction: apparent path collision invalidated

A constructed path experiment initially appeared to produce equal frozen certificate histories with different independently constituted path consequences.

Localization found the first equality boundary earlier than SSI certification:

```text
external path difference
-> pre-certificate experiment mapping collapse
-> equal SSI-local inputs
-> equal local certificates
```

The SSI-local cases had been constructed independently rather than derived from the external path records. Therefore the result does not establish path-level SSI inadequacy.

Current status:

```text
FIRST_ECDSA_RESULT
    = INVALID_AS_PATH_COLLISION
      __PRE_CERTIFICATE_MAPPING_COLLAPSE

PATH_LEVEL_SSI_INADEQUACY
    = NOT_ESTABLISHED

CERTIFICATE_PROJECTION_LOSS
    = NOT_TESTED

RELATIONAL_COMPOSITION_FAILURE
    = NOT_TESTED

LOCAL_COMPOSITION_REOPENING
    = NOT_EARNED_FROM_THAT_WITNESS
```

No frozen SSI checker artifact was repaired as a consequence of that diagnosis.

---

## PR67 — external-to-SSI mapping boundary

PR67 freezes the previously missing experiment-constitution interface:

```text
M_F : E(T) -> I_F(T)
```

The mapper recognizes only explicit dispositions:

```text
PRESERVE
NORMALIZE
EXCLUDE_WITH_BASIS
NOT_EVALUABLE
```

and enforces:

```text
excluded from I_F != globally irrelevant
unmappable != irrelevant
unknown field != safe to drop
A/B input equality = derived result only
```

The mapper cannot consume downstream path consequence, certificate output, path-output, collision expectation, or desired diagnosis information.

Frozen mapper-only result on PR67:

```text
12 / 12 obligations passed
```

Strongest bounded claim:

```text
EXTERNAL_TO_SSI_MAPPING_CONSTITUTION
    = SUPPORTED_ON_FROZEN_ABSTRACT_MAPPING_SUITE
```

Authority ceiling remains:

```text
PATH_LEVEL_INADEQUACY          = NOT_ESTABLISHED
CERTIFICATE_PROJECTION_LOSS    = NOT_TESTED
RELATIONAL_COMPOSITION_FAILURE = NOT_TESTED
INTERFACE_AUTHORITY_BOUNDARY   = UNKNOWN
COMPOSITION_THEOREM            = NOT_EARNED
NEW_COORDINATE                 = NOT_EARNED
REPAIR                         = NOT_EARNED
SSI_CALC_KERNEL_DELTA          = 0
```

---

## Path-run dependency closure

The next path experiment may run only after the complete dependency closure is constituted as a self-contained, independently resolvable artifact set.

```text
D_R = {
    M_F,
    P_A,
    P_B,
    independent Y_path evaluator,
    one-shot runner,
    pre-execution freeze manifest,
    exact SSI checker dependencies
}
```

Every dependency must be:

```text
IDENTIFIED
AND IMMUTABLE
AND RESOLVABLE_BY_EXECUTOR
```

Current status:

```text
COMPLETE_PATH_RUN_BUNDLE
    = NOT_CONSTITUTED

PATH_RUN_AFTER_M_F
    = NOT_EXECUTED
```

A failed executor attempt caused by unresolved artifacts is `NOT_EVALUABLE`; it is not a negative scientific result.

Important distinction:

```text
artifact exists
    != artifact is addressable
    != artifact is reachable by the executor
```

The execution boundary is currently treated as a workflow/reproducibility variable, not a new SSI primitive.

---

## PR63–65 status

### PR63 — adequacy governor

The gate preserves:

```text
SUPPORTED_ADEQUATE_ON_TESTED_SCOPE
UNKNOWN
INADEQUATE
NOT_EVALUABLE
```

Only `INADEQUATE` licenses local search. No repair generation or SSI-CALC modification is authorized.

### PR64 / H9

Frozen hostile first result: `8/10`.

H9 establishes on its frozen scope:

```text
coverage assertion != coverage warrant
```

and supports local positive-adequacy constitution nonidentifiability.

### PR65

Frozen one-bit candidate search:

```text
NO_EXACT_CANDIDATE
exact_candidates = []
repair_permitted = false
new_ssi_coordinate_established = false
SSI_CALC_KERNEL_DELTA = 0
```

`C1 AND C4` is an exact extensional fit on the frozen ten-world domain only. Criterion sufficiency and mechanism identification remain unestablished.

---

## PR59–61 transition spine

```text
PR59
FOUR_COORDINATE_TRANSITION_INTERFACE_SEPARABILITY
    = SUPPORTED_ON_FROZEN_CONSTRUCTED_SUITE

PR60
TESTED_STRUCTURAL_TRANSPORT
    = SUPPORTED_ON_EVALUABLE_FROZEN_HELDOUT_AXES
G2_OBSERVATION_CHANNEL_NOVELTY
    = NOT_EVALUABLE_UNDER_FROZEN_BINDING

PR61
TRANSITION_SEMANTIC_TRANSPORT
    = SUPPORTED_ON_FROZEN_CONSTRUCTED_CROSS_CARRIER_SUITE
```

The non-rule still governs:

```text
local preservation != path preservation
```

but it does not itself establish a composition defect.

---

## SSI-CALC status

The SSI-CALC kernel remains frozen:

```text
|K| = 11
R12 = NOT_EARNED
R13 = NOT_EARNED
SSI_CALC_KERNEL_DELTA = 0
```

Neither H9, the failed path witness, nor PR67 grants authority to add a kernel rule or coordinate.

---

## Current research stop

```text
M_F
    = FROZEN_ON_PR67
      __SUPPORTED_ON_FROZEN_ABSTRACT_MAPPING_SUITE

PATH_HYPOTHESIS
    = LIVE

COMPLETE_PATH_RUN_BUNDLE
    = NOT_CONSTITUTED

PATH_COLLISION
    = NOT_TESTED_UNDER_CONSTITUTED_MAPPING

COMPOSITION_INSUFFICIENCY
    = NOT_ESTABLISHED

COMPOSITION_THEOREM
    = NOT_EARNED

NEW_RELATION
    = NOT_EARNED

NEW_COORDINATE
    = NOT_EARNED

LEVEL_3_INTERFACE_INVENTION
    = OPEN

SSI_NOVELTY
    = NOT_ESTABLISHED

SSI_CALC_KERNEL_DELTA
    = 0
```

Next work is packaging only:

```text
SELF_CONTAINED_PATH_RUN_DEPENDENCY_BUNDLE
```

No path execution should occur until that bundle is frozen and independently resolvable.

---

## Governing methodological rules

```text
Constitute the witness at every interface it is supposed to falsify.

Excluded from local input != irrelevant externally.

A/B equality at a tested interface must be derived, never manufactured.

Research-state assertion != reproducibly accessible artifact.

Experimental provenance is incomplete until the full dependency closure is independently resolvable.

NOT_EVALUABLE != NOT_SUPPORTED.

When evidence stops identifying, authority stops with it.
```

---

## Administrative vs scientific files

Reader-facing files such as:

```text
README.md
RESEARCH_MAP.md
REPOSITORY_STATUS.md
research/CURRENT_FRONTIER.md
research/SSI_BIG_PICTURE.md
research/relicense/README.md
```

are navigation/synthesis layers. They do not outrank frozen experiments.

> **Newest file wins is not an epistemic rule.**
