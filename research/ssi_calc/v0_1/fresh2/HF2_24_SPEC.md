# SSI-CALC v0.1 — HF2-24 Post-Substrate Fresh Terrain

## Role

`HF2-24` is constituted **after** the typed live-authority successor-v2 was frozen on `main` and **before** that successor is executed on these cases.

Permanent epistemic roles:

```text
B64    = contract conformance
H24    = known-failure regression
HF16   = known-failure regression
HF2-24 = post-substrate fresh white-box generalization
```

HF2-24 can support or falsify only a post-substrate **white-box** generalization claim. It cannot establish external niche advantage.

## Frozen predecessor

```text
SUCCESSOR_V2_FREEZE_MERGE = ed9ae7ab94f74bbc39aaadad312c40647aed62d9
KERNEL_RULE_COUNT         = 11
R12_AUTHORIZED            = NO
```

## Corpus

```text
CASE_COUNT       = 24
FAMILIES         = 8
CASES_PER_FAMILY = 3
LICENSED         = 12
NON_AUTHORIZED   = 12
CASE_IDS         = CASE-301..CASE-324
```

The cases target new **compositions and placements** of already-earned distinctions rather than replaying H24/HF16 witnesses verbatim.

Fresh stressors include:

- direct live authority coexisting with irrelevant historical provenance;
- deeper information-flow ancestry;
- three-hop transfer chains and partial chain certificates;
- mixed-jurisdiction component composition;
- live identity authority with withdrawn reference noise;
- foreign-but-live identity authority;
- generic typed bridge target matching;
- support requiring lineage + live bridge + consumer-scoped edge;
- live carrier comparison with unresolved purpose evidence;
- desired-result-selected transport despite otherwise adequate transport structure;
- active commutation counterexamples;
- **alternative sufficient proof paths** where one live proof should not be blocked by an unresolved unused proof channel;
- active future distinctions defeating withdrawn local reassurance.

## Core substrate hypothesis under test

The successor-v2 claims to enforce:

```text
EXISTS != LIVE != DISCHARGES
```

Operationally:

```text
historical lineage
  -> typed live-authority projection
  -> obligation discharge
  -> R1..R11 adjudication
```

HF2-24 tests whether that separation generalizes beyond the exact H24/HF16 repair witnesses.

## Firewall

Before first exposure:

```text
SUCCESSOR_EXECUTED_ON_HF2_24 = FALSE
```

After this freeze merges:

```text
DELTA_HF2_24_AFTER_EXPOSURE       = 0
DELTA_EXPECTED_AFTER_EXPOSURE     = 0
DELTA_SUCCESSOR_DURING_FIRST_RUN  = 0
DELTA_METRICS_AFTER_EXPOSURE      = 0
```

The first result must be preserved before any later repair.

## Prospective result labels

Using authorize/refuse decision, overreach, and false-refusal axes:

```text
HF2_24_STRONG_PASS
  decision = 24/24
  overreach = 0
  false_refusal = 0

HF2_24_PASS
  decision >= 0.875
  overreach <= 0.125
  false_refusal <= 0.125

HF2_24_PARTIAL
  decision >= 0.75
  but PASS thresholds fail

HF2_24_FAILED
  decision < 0.75
  OR overreach > 0.25
```

Exact status, shallowest locus, preserved authority, missing authority, reopened set, and exact certificate remain secondary diagnostic metrics and are reported separately.

## Authority ceiling

Even `HF2_24_STRONG_PASS` implies at most:

```text
POST_SUBSTRATE_WHITEBOX_GENERALIZATION_SUPPORTED_IN_HF2_24
```

It does **not** imply formal completeness, external validity, or niche advantage over independent baselines.
