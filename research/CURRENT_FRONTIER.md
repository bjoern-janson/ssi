# SSI Current Frontier

Status: **reader-facing research status / non-authoritative synthesis**  
Date: **2026-08-17**

Frozen experiment artifacts, first-result ledgers, exact commits, and their authority ceilings govern if this file conflicts with them.

---

## 1. Current state

The repository has advanced beyond the old `POST_PR61_FRONTIER = UNCONSTITUTED` navigation state, but it has **not** established path-level composition insufficiency.

The current bounded lineage is:

```text
PR59  transition-role separation
  -> PR60 held-out structural transport
  -> PR61 semantic / evidential transport
  -> PR63 adequacy / reopening governor
  -> PR64 H9 positive-adequacy constitution collision
  -> PR65 negative one-bit candidate search
  -> PR67 external-to-SSI mapping contract M_F [draft / branch-local]
  -> CURRENT STOP: complete path-run dependency closure not constituted
```

PR63–65 are already mainline-reachable through the consolidated history. PR67 remains an open draft and is not promoted here into mainline authority.

---

## 2. What PR63–65 added

### PR63 — adequacy governor

A narrow executable gate distinguishes:

```text
SUPPORTED_ADEQUATE_ON_TESTED_SCOPE
UNKNOWN
INADEQUATE
```

with `NOT_EVALUABLE` kept separate as a protocol/constitution status. Only `INADEQUATE` licenses local search. The gate does not generate repairs, invent coordinates, grant execution authority, or modify SSI-CALC.

### PR64 / H9 — constitution collision

The hostile suite preserved its first 8/10 result. H9 established a local positive-adequacy constitution problem:

```text
coverage assertion != coverage warrant
```

The executable representation could not distinguish independently constituted positive coverage from caller-supplied assertion at that boundary.

### PR65 — candidate search

Five one-bit candidate repairs were frozen and tested. Result:

```text
NO_EXACT_CANDIDATE
repair_permitted = false
new_ssi_coordinate_established = false
SSI_CALC_KERNEL_DELTA = 0
```

Ledger analysis found `C1 AND C4` to be an exact extensional fit on the frozen ten-world domain only. Sufficiency and mechanism remain unestablished.

---

## 3. The path experiment correction

A later constructed path experiment initially appeared to satisfy:

```text
Psi_F(P_A) == Psi_F(P_B)
AND
Y_path(P_A) != Y_path(P_B)
```

Localization invalidated that interpretation as an SSI path collision.

The first equality boundary was earlier:

```text
external paths differ
    -> experiment mapper constructs equal SSI-local inputs
    -> equal local inputs
    -> equal certificate histories
```

The external-to-SSI mapper had constructed corresponding SSI inputs independently of the external path records. Therefore the apparent equality of frozen certificate histories was downstream of a **pre-certificate experiment-constitution collapse**.

Current correction:

```text
FIRST_ECDSA_RESULT
    = INVALID_AS_PATH_COLLISION
      __PRE_CERTIFICATE_MAPPING_COLLAPSE

PATH_LEVEL_SSI_INADEQUACY
    = NOT_ESTABLISHED_BY_THAT_WITNESS

CERTIFICATE_PROJECTION_LOSS
    = NOT_TESTED

RELATIONAL_COMPOSITION_FAILURE
    = NOT_TESTED

LOCAL_COMPOSITION_REOPENING
    = NOT_EARNED_FROM_THAT_WITNESS
```

The methodological rule earned by the diagnosis is:

> **Constitute the witness at every interface it is supposed to falsify.**

---

## 4. PR67 — external-to-SSI mapping contract

PR67 freezes the missing experimental boundary:

```text
M_F : E(T) -> I_F(T)
```

where the mapper makes an explicit jurisdictional disposition for external information:

```text
PRESERVE
NORMALIZE
EXCLUDE_WITH_BASIS
NOT_EVALUABLE
```

with the hard non-rule:

```text
excluded from I_F != irrelevant to an external path consequence
```

The mapper may not consume downstream path consequences, SSI certificate outputs, frozen path outputs, collision expectations, or desired diagnoses.

PR67 first mapper-only exposure:

```text
12 / 12 frozen abstract mapping obligations passed
```

Strongest branch-local earned claim:

```text
EXTERNAL_TO_SSI_MAPPING_CONSTITUTION
    = SUPPORTED_ON_FROZEN_ABSTRACT_MAPPING_SUITE
```

PR67 does **not** establish path inadequacy, certificate projection loss, relational composition failure, or a new SSI coordinate.

---

## 5. Current execution boundary

The next path test is methodologically meaningful only if its complete dependency closure is independently resolvable.

Let:

```text
D_R = {
    M_F,
    P_A,
    P_B,
    Y_path evaluator,
    one-shot runner,
    freeze manifest,
    exact SSI dependencies
}
```

Evaluation requires, for every dependency:

```text
identified
AND immutable
AND resolvable by the executor
```

A research-state assertion that an artifact exists is not sufficient.

```text
artifact exists
    != artifact is addressable
    != artifact is reachable by a particular executor
```

A frozen experiment is therefore not reproducible merely because its provenance is described; the complete dependency closure must be independently resolvable.

The operational packaging rule is:

> **If packaging the experiment requires reconstructing scientific state, the package is not yet a valid experimental artifact.**

This is currently a workflow/reproducibility constraint, not a new SSI primitive.

---

## 6. Current stop

No post-`M_F` path run has been executed under a complete self-contained dependency bundle.

```text
M_F
    = FROZEN_ON_PR67
      __SUPPORTED_ON_FROZEN_ABSTRACT_MAPPING_SUITE

COMPLETE_PATH_RUN_BUNDLE
    = NOT_CONSTITUTED

PATH_RUN_AFTER_M_F
    = NOT_EXECUTED

PATH_COLLISION
    = NOT_TESTED_UNDER_CONSTITUTED_MAPPING

CERTIFICATE_PROJECTION_LOSS
    = NOT_TESTED

RELATIONAL_COMPOSITION_FAILURE
    = NOT_TESTED

INTERFACE_AUTHORITY_BOUNDARY
    = UNKNOWN

COMPOSITION_INSUFFICIENCY
    = NOT_ESTABLISHED

COMPOSITION_THEOREM
    = NOT_EARNED

NEW_RELATION
    = NOT_EARNED

NEW_COORDINATE
    = NOT_EARNED

REPAIR
    = NOT_EARNED

LEVEL_3_INTERFACE_INVENTION
    = OPEN

SSI_NOVELTY
    = NOT_ESTABLISHED

SSI_CALC_KERNEL_DELTA
    = 0
```

The path hypothesis remains live. The current execution route is blocked by experimental constitution/reproducibility, not by a scientific negative result.

---

## 7. Next scientific increment

The next work product is deliberately boring:

```text
SELF_CONTAINED_PATH_RUN_DEPENDENCY_BUNDLE
```

It should contain bytes + manifest + hashes + one runner, with no dependency on conversation memory, implicit branch knowledge, connector-specific hidden state, or manual reconstruction.

Until that bundle is frozen and independently resolvable:

```text
NO PATH EXECUTION
NO CHECKER REPAIR
NO NEW COORDINATE
NO COMPOSITION CLAIM
```

---

## 8. Governing methodological compressions

```text
A falsification witness must be constituted all the way to the interface being falsified.

Constitute the witness at every interface it is supposed to falsify.

Excluded from a local interface != globally irrelevant.

Research-state assertion != reproducibly accessible artifact.

Experimental provenance is incomplete until the full dependency closure is independently resolvable.

When evidence stops identifying, authority stops with it.
```
